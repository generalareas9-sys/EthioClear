# =====================================================================
# EthioClear Backend — Environment Configuration Template
# Copy this file to `.env` and fill in real local values.
# NEVER commit the actual `.env` file to version control.
# =====================================================================

# ---------------------------------------------------------------------
# Server
# ---------------------------------------------------------------------
NODE_ENV=development
PORT=5000
API_BASE_PATH=/api

# ---------------------------------------------------------------------
# Frontend origin(s) allowed by CORS (comma-separated for multiple)
# ---------------------------------------------------------------------
CLIENT_ORIGIN=http://localhost:5173

# ---------------------------------------------------------------------
# PostgreSQL Database
# ---------------------------------------------------------------------
PGHOST=localhost
PGPORT=5432
PGUSER=ethioclear_user
PGPASSWORD=change_me
PGDATABASE=ethioclear_db
# Optional: full connection string overrides the discrete PG* vars above
# DATABASE_URL=postgresql://ethioclear_user:change_me@localhost:5432/ethioclear_db
PG_SSL=ture

# ---------------------------------------------------------------------
# JWT Authentication (used starting Module 4)
# ---------------------------------------------------------------------
JWT_SECRET=replace_with_a_long_random_secret
JWT_EXPIRES_IN=1h
JWT_REFRESH_SECRET=replace_with_a_different_long_random_secret
JWT_REFRESH_EXPIRES_IN=7d

# ---------------------------------------------------------------------
# Bcrypt
# ---------------------------------------------------------------------
BCRYPT_SALT_ROUNDS=12

# ---------------------------------------------------------------------
# Rate Limiting
# ---------------------------------------------------------------------
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# ---------------------------------------------------------------------
# File Storage
# ---------------------------------------------------------------------
UPLOAD_DIR=storage/uploads
CERTIFICATE_DIR=storage/certificates
TEMP_DIR=storage/temp
MAX_UPLOAD_SIZE_MB=5

# ---------------------------------------------------------------------
# Certificate Verification (internal only — never a government URL)
# ---------------------------------------------------------------------
VERIFICATION_BASE_URL=http://localhost:5173/verify
