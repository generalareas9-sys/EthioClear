# Database Documentation

See ../src/database/schema.sql for the authoritative schema. This file will describe each table's purpose, relationships, and key constraints in full once all modules are complete.
