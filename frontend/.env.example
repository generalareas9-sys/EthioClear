# =====================================================================
# EthioClear Frontend — Environment Configuration Template
# Copy this file to `.env` and adjust if your backend runs elsewhere.
# =====================================================================

# Base URL of the EthioClear backend API (see services/api.js).
VITE_API_BASE_URL=http://localhost:5000/api
